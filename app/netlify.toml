[build]
  command = "npm install && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  EXPO_PUBLIC_API_URL = "https://api.getkonta.app/api/v1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
