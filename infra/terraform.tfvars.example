# AWS Region (eu-west-3 = Paris)
aws_region = "eu-west-3"

# Environment
environment = "production"

# Project name (used for resource naming)
project_name = "konta"

# Database Configuration
db_name     = "finance_manager"
db_username = "postgres"
db_password = "CHANGE_ME_TO_STRONG_PASSWORD"  # At least 16 characters

# RDS Configuration
# db.t4g.micro = ~$13/month (2 vCPU, 1GB RAM)
# db.t4g.small = ~$26/month (2 vCPU, 2GB RAM)
db_instance_class = "db.t4g.micro"

# EC2 Configuration
# t3.micro  = ~$7.50/month (1GB RAM) - may struggle with Docker builds
# t3.small  = ~$15/month (2GB RAM) - recommended
# t3.medium = ~$30/month (4GB RAM) - for heavier workloads
ec2_instance_type = "t3.small"

# SSH Public Key for EC2 access
# Generate with: ssh-keygen -t ed25519 -f ~/.ssh/konta-key -C "konta-ec2"
ssh_public_key = "ssh-ed25519 AAAA... konta-ec2"

# Cloudflare Configuration
# Create token at: https://dash.cloudflare.com/profile/api-tokens
# Use "Edit zone DNS" template, select your zone
cloudflare_api_token = "YOUR_CLOUDFLARE_API_TOKEN"
cloudflare_zone_name = "getkonta.app"
